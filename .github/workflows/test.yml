name: Test CI

on:
  push:
    branches: [main]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run tests
        run: |
          echo "Test CI running on branch ${{ github.ref_name }}"
          echo "No test suite configured yet - add your test commands here."
